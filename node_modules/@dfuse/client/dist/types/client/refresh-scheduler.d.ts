export declare type ScheduleJob = () => void;
export interface RefreshScheduler {
    release(): void;
    hasScheduledJob(): boolean;
    schedule(delayInSeconds: number, job: ScheduleJob): void;
}
export declare function createRefreshScheduler(): RefreshScheduler;
